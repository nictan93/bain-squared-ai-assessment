// CRM tags for the AI Assessment are built as part of FullResult.crmTags in scoring.ts.
// This module is a stub kept for import compatibility.

export function deriveTags(): string[] {
  return [];
}
